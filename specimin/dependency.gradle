apply plugin: 'base'

repositories {
    mavenCentral()
}

configurations {
    toCopy
}

dependencies {
    toCopy 'org.checkerframework:checker-qual:3.1.0'
}

task pullJar(type: Copy) {
    from configurations.toCopy 
    into 'libs'
}